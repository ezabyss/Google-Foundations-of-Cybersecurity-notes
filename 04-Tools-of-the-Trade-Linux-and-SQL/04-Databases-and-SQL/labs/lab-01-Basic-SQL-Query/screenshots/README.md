# Screenshots – Perform a SQL Query

This directory contains screenshots captured during the execution of the SQL lab **“Perform a SQL Query”**, completed using the MariaDB shell.

These screenshots serve as **visual evidence** of successfully querying and analyzing data from the `organization` database.

---

## Screenshot Checklist and Filenames

### Task 1: Retrieve Employee Device Data
- **task-01-select-all-machines.png**  
  Shows `SELECT * FROM machines;`

- **task-01-select-device-email-client.png**  
  Shows selection of `device_id` and `email_client` columns

- **task-01-select-os-patch-date.png**  
  Shows selection of `device_id`, `operating_system`, and `OS_patch_date`

---

### Task 2: Investigate Login Activity
- **task-02-select-event-country.png**  
  Shows selection of `event_id` and `country` from `log_in_attempts`

- **task-02-select-username-date-time.png**  
  Shows selection of `username`, `login_date`, and `login_time`

- **task-02-select-all-login-attempts.png**  
  Shows `SELECT * FROM log_in_attempts;`

---

### Task 3: Order Login Attempts Data
- **task-03-order-by-login-date.png**  
  Shows login attempts ordered by `login_date`

- **task-03-order-by-date-time.png**  
  Shows login attempts ordered by `login_date` and `login_time`

---

