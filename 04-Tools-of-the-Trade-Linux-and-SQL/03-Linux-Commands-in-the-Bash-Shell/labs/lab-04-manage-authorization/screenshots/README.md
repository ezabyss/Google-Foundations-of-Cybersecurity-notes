## Screenshots Documentation

This directory contains screenshots that verify the successful completion of **Lab 04: Manage Authorization with Linux Permissions**.  
Each screenshot provides visual evidence of examining and modifying file and directory permissions as required in the lab.

---

## Screenshot List

- **Task-01-check-permissions.png**  
  Shows the use of `ls -l` and `ls -la` in the `/home/researcher2/projects` directory to examine file and directory permissions, including hidden files.

- **Task-02-change-file-permissions.png**  
  Displays the modification of file permissions using `chmod`, including removing write access for other users and restricting group permissions on sensitive files.

- **Task-03-hidden-file-permissions.png**  
  Shows checking and updating permissions on the hidden file `.project_x.txt` to remove unauthorized write access while preserving read access.

- **Task-04-directory-permissions.png**  
  Displays permission changes on the `drafts` directory, confirming that only the `researcher2` user has access.

---


## Purpose

These screenshots serve as execution evidence for the lab and demonstrate correct handling of Linux file and directory permissions. This documentation validates the ability to enforce authorization and apply the principle of least privilege in a Linux environment.
