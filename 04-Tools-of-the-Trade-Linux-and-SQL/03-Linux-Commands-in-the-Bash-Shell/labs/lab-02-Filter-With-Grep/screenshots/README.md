## Screenshots Documentation

This directory contains screenshots that verify the successful execution of **Lab 02: Filter with grep**.  
Each screenshot corresponds to one major task performed during the lab and provides visual evidence of command execution and output.

---

## Screenshot List

- **Task-01-grep-error.png**  
  Shows navigation to `/home/analyst/logs` and the use of the `grep` command to filter and display error messages from the `server_logs.txt` file.

- **Task-02-ls-grep.png**  
  Displays the use of piping (`ls | grep`) to filter filenames in the `/home/analyst/reports/users` directory that contain specific strings such as `Q1` or `access`.

- **Task-03-cat-grep.png**  
  Shows searching within user report files using `grep` to locate specific usernames or department information.

---

## Purpose

These screenshots provide concise execution evidence for the lab and demonstrate effective use of the `grep` command and piping to filter file contents and filenames in a Linux environment. This documentation supports hands-on learning and validates task completion.
