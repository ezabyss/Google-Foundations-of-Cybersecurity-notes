## Screenshots Documentation

This directory contains screenshots that verify the successful completion of **Lab 03: Manage files with Linux commands**.  
Each screenshot corresponds to a major task performed during the lab and provides visual proof of command execution and results.

---

## Screenshot List

- **Task-01-create-logs.png**  
  Shows the creation of the `logs` directory in `/home/analyst` and confirms the updated directory listing.

- **Task-02-remove-temp.png**  
  Displays the removal of the `temp` directory and verifies that it no longer exists.

- **Task-03-move-q3patches.png**  
  Shows moving `Q3patches.txt` from the `notes` directory to the `reports` directory and confirms the updated reports listing.

- **Task-04-remove-tempnotes.png**  
  Displays the removal of the `tempnotes.txt` file from the `notes` directory.

- **Task-05-create-tasks-file.png**  
  Shows the creation of the `tasks.txt` file inside the `notes` directory.

- **Task-06-edit-tasks-file.png**  
  Displays the contents of `tasks.txt` after editing with the `nano` text editor, confirming that the task information was saved successfully.

---


